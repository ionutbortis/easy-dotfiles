#!/bin/bash

# The 'SUPPORTED_DISTROS' configuration should be left as a bash array 
# and its elements should be STRICTLY only letters and numbers.
# No spaces, dashes or other characters are allowed!
SUPPORTED_DISTROS=( "fedora" "ubuntu" )

DEFAULT_HOST_NAME="myPC"

DEFAULT_GIT_USERNAME="my_username"
DEFAULT_GIT_EMAIL="my_email_address"
